package com.example.riccde.jkl;


import android.content.Intent;
import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;
import android.view.View;
import android.widget.ArrayAdapter;
import android.widget.ListView;
import android.widget.Toast;

import java.util.ArrayList;

public class ListActivity extends AppCompatActivity{
 /*
    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_list);

        String[] values = new String[]{"Apple", "Banana", "Orange", "Tomato", "Bacon"};
        ListView list = (ListView) findViewById(R.id.MainList);

        Bundle extras = getIntent().getExtras();
        ArrayList<String> tmp = extras.getStringArrayList("myList");

        if (addList.size() != 0) {
            ArrayAdapter<String> adapter = new ArrayAdapter<String>(list.getContext(), android.R.layout.simple_list_item_1, tmp);
            list.setAdapter(adapter);
            Toast.makeText(this, "List is completed.", Toast.LENGTH_SHORT).show();
        }
        else {
            Toast.makeText(this, "Nothing", Toast.LENGTH_SHORT).show();
        }


    }*/
}
